rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admin scripts (like our seeder) to bypass all rules
    // But deny all public read/write access by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
