rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // By default, deny all client-side reads and writes.
    // Server-side admin SDK access is not affected by these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
